# ─────────────────────────────────────────────────────────────
# WEB APP — PRODUCTION ENVIRONMENT
# Copy this to .env on the server:
#   /var/www/ecokuku/apps/web/.env
# Then fill in the values below and restart PM2.
# ─────────────────────────────────────────────────────────────

# Database (production — same DB as admin)
DATABASE_URL="postgresql://postgres:PASSWORD@localhost:5432/ecokuku_prod"

# NextAuth
NEXTAUTH_SECRET="bjHyxIQjUs2S0QnuVVIDNoLqAWKLdth-3f2ICBnzug7Zw3GvzOrDoTfZwDbYTsBz8FbUZF_3XAHukqvV0xSL9g"
NEXTAUTH_URL="http://167.172.110.41:3000"
# If you add a domain later: NEXTAUTH_URL="https://yourdomain.com"

# Admin app public URL — used to resolve legacy /uploads/ image paths
# If using Nginx with a subdomain: NEXT_PUBLIC_ADMIN_URL="https://admin.yourdomain.com"
# Otherwise keep the server IP + port:
NEXT_PUBLIC_ADMIN_URL="http://167.172.110.41:3001"

NODE_ENV="production"
